<section class="page-categories-index">
  <h3 class="page-title">Categories</h3>

  <%= render 'categories/list' %>

  <% if logged_in? %>
    <%= link_to 'Add New Category', new_category_path, class: 'btn btn-primary btn-md' %>
  <% end %>
</section>
